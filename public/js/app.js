$(document).ready(function () {
  $('.button-collapse').sideNav({
    closeOnClick: true,
    draggable: true
  });

  $('.carousel.carousel-slider').carousel({ fullWidth: true });

  $('.slider').slider();
});
